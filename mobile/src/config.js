// ── EDIT-ME ──────────────────────────────────────────────────────────
// Backend base URL. The app calls `<MGT_API_BASE>/api/...`.
// Native builds MUST point at the deployed HTTPS backend (no same-origin).
// Change this to your production API origin before shipping.
window.MGT_API_BASE = window.MGT_API_BASE || "https://motogiathinh.centersai.com";
// ─────────────────────────────────────────────────────────────────────
