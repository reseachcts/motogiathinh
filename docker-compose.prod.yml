services:
  backend:
    volumes: []
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4

  celery_worker:
    volumes: []

  celery_beat:
    volumes: []

  frontend:
    volumes: []
    environment:
      - NODE_ENV=production
